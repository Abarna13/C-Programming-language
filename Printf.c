#include<stdio.h>
int main()
{
    int two = 2, three = 3, six = 6;
    int result = (two + three) * six / three;
    printf("I am new to programming but i known how to do arithematic operations like this: (%d + %d) * %d / %d", two, three, six, three);
    printf("\nResult is %d", result);

} 